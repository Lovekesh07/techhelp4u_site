import AboutSection from "../sections/AboutSection";

// Standalone subpage at /about — same content as the About section on the
// home page, for anyone who wants to link directly to it.
export default function About() {
  return <AboutSection />;
}
