import ServicesSection from "../sections/ServicesSection";

// Standalone subpage at /services — same content as the Services section on
// the home page, for anyone who wants to link directly to it.
export default function Services() {
  return <ServicesSection />;
}
